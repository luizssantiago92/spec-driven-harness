# Token-cost fixtures

Three named profiles (`naive`, `specify`, `execute`) pin progressive-loading
savings against **real packaged skill files** listed in `lib/token-cost.js`
`LOAD_PROFILES`.

`profiles.json` stores expected token bands. The suite measures live package
assets so README numbers stay honest without vendoring duplicate markdown trees.
