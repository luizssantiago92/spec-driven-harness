---
description: Engineering baseline and harness map for this repository
alwaysApply: true
---

# Engineering Baseline

- Follow existing codebase conventions before introducing new patterns.
- Run tests and linters before considering work done.
- Never commit secrets, API keys, or credentials.
- Prefer small, focused diffs over large refactors.
- Touch only the files the current task requires; park out-of-scope ideas in `.specs/STATE.md`.
- Apply secure coding practices from `.cursor/skills/engineering-standards.md`.
- Run the security checklist from `.cursor/skills/security-review.md` during `/verify`.

# Artifact Language

All project artifacts are written in English: source code, tests, comments, commit messages, PR titles and descriptions, `.specs/` documents, and identifier names.

Chat language is a personal preference. Set it in your own agent settings if you want replies in another language.

# Harness Skills

<!-- harness-managed:skills-map:start -->
| Skill | Purpose |
| --- | --- |
| `.cursor/skills/agent-architecture.md` | SDD hub — contract, phases, gates, complexity router |
| `.cursor/skills/references/` | Phase procedures (explore, constitution, specify, discuss, design, tasks, analyze, implement, validate, converge, archive, memory, quick-mode, context-limits, lessons, sub-agents) |
| `.cursor/skills/task-graph-engineering.md` | Task DAG, parallelism, verify topology |
| `.cursor/skills/engineering-standards.md` | Code quality, secure coding, git hygiene |
| `.cursor/skills/security-review.md` | Security checklist for verification |
| `.cursor/skills/appsec.md` | Application security sister (load on demand) |
| `.cursor/skills/qa-strategy.md` | QA strategy sister (load on demand) |
| `.cursor/skills/code-simplify.md` | Simplification sister (load on demand) |
| `.cursor/skills/ship-ready.md` | Ship-ready sister (load on demand) |
| `.cursor/skills/git-handoff.md` | Git sync and session handoff |
<!-- harness-managed:skills-map:end -->

# Deterministic Gates

<!-- harness-managed:gates-map:start -->
Structural gates live in `.specs/harness/scripts/` and require Python 3. A non-zero exit means STOP.

| When | Command |
| --- | --- |
| Before `/specify` (Medium+) | `npx @luizsantiago/agentic-harness feature-init "<description>"` (Tier 0 — folder + branch + STATE) |
| Before confirming a spec | `python3 .specs/harness/scripts/validate_spec.py [feature]` |
| Before approving tasks | `python3 .specs/harness/scripts/analyze_artifacts.py [feature]` then `validate_tasks.py [feature]` |
| On each commit | `python3 .specs/harness/scripts/check_commit.py --message "<message>"` |
| Before declaring done | `python3 .specs/harness/scripts/validate_state.py [feature]` |
| After a grounded verify FAIL | `python3 .specs/harness/scripts/lessons.py add --source .specs/features/[feature]/validation.md` |

Gates accept a feature name, a feature directory, or a path to the artifact. With no argument they auto-detect when the project has exactly one feature; with several they list candidates and exit 2.

If Python is unavailable, perform the same checks by reading the artifact against the reference checklist.

**Git tiers:** Tier 0 (feature-init, local branch, commits) runs on phase triggers. Tier 1 (`git push`, PR) and Tier 2 (merge, deploy, force-push) need explicit owner go-ahead. See `git-handoff.md`.

Persistent state: `.specs/STATE.md` (decisions/handoff), `.specs/lessons.json` (canonical lessons), `.specs/LESSONS.md` (generated playbook). Commit `.specs/` via `/handoff` — see `git-handoff.md`.
<!-- harness-managed:gates-map:end -->
